@model IEnumerable<TmdLesson09.Models.TmdKhoa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TmdTenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TmdTenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TmdMaKH }) |
            @Html.ActionLink("Details", "Details", new { id=item.TmdMaKH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TmdMaKH })
        </td>
    </tr>
}

</table>
